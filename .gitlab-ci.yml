stages:
  - test

variables:
    DOCKER_HOST: tcp://docker:2375/
    DOCKER_DRIVER: overlay2
    CROSS_REMOTE: 1
    DOCKER_TLS_CERTDIR: ""

services:
    - docker:dind

test:
  stage: test
  image: haelty/rust-test-cross
  script:
    - apt-get install -y nmap
    - nmap -sT -p- docker

